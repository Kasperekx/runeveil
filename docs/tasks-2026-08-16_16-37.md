# Tasks: Wspólne chrome paneli, redesign dialogu, fundament questowy

Date: 2026-08-16_16-37

Plan: `docs/plan-2026-08-16_16-37.md`

## Etap 1 — wspólne chrome

- [ ] 1. `public/styles/panel.css`: `.panel`, `__frame`, `__corner--tl/tr/bl/br`,
      `__header`, `__sigil`, `__titles`, `__eyebrow`, `__title`, `__rule`,
      `__close`, `__body`, `__heading`, `__chip` + tokeny `--panel-*`
- [ ] 2. `index.html`: podłączyć `panel.css` przed pozostałymi plikami paneli
- [ ] 3. `character-panel.css`: usunąć skopiowane chrome, zostawić pozycję,
      szerokość i delty jako `.character-panel .panel__*`
- [ ] 4. `CharacterPanel.ts`: markup na klasy `panel__*`
- [ ] 5. `quest-log.css` + `QuestLog.ts`: to samo
- [ ] 6. `SkillsPanel.ts` + `ProfessionsPanel.ts`: markup na `panel__*`, wyciąć
      z `style.css` ich zduplikowane chrome
- [ ] 7. Wizualna weryfikacja: otworzyć C / Q / K / L, sprawdzić brak regresji
      ramek, narożników, przeciągania i animacji zamykania

## Etap 2 — dialog

- [ ] 8. `DialogueWindow.ts`: markup na `panel__*`, usunąć `__ornament`,
      tożsamość NPC do nagłówka (`eyebrow` = rola, `title` = imię), złoto jako
      `panel__chip`, wyciąć blok `__identity`
- [ ] 9. `DialogueWindow.ts`: ciało na dwie kolumny (portret | tekst + treść
      trybu); tryb podmienia tylko prawą kolumnę
- [ ] 10. `repair-workshop__*` → `dialogue-window__service*` na `panel__heading`,
      usunąć blok `__hero`
- [ ] 11. `style.css`: usunąć wariant `dialogue-window--repair` (szerokość, tło,
      sigil) i całą sekcję starego chrome dialogu
- [ ] 12. `LootWindow.ts` + jego CSS: przejście na `panel__*`, usunąć `__ornament`
- [ ] 13. Weryfikacja: rozmowa, handel (Kup/Sprzedaj), naprawa, powrót do
      rozmowy, przeciąganie okna, zamykanie Esc

## Etap 3 — fundament questowy

- [ ] 14. `shared/quests/parse.ts`: `parseQuests(raw)` + typy, walidacja rzucająca
      na niepoprawny wpis
- [ ] 15. `src/content/quests.ts` i `server/src/content/questConfig.ts`: użyć
      wspólnego parsera, usunąć zduplikowaną walidację (koniec `return []` vs `throw`)
- [ ] 16. `quests.yaml`: dodać `text: { offer, active, turnIn }` do obu zadań
- [ ] 17. `PlayerState`: pole `availableQuests: { array: "string" }` (niepersystowane)
- [ ] 18. `QuestSystem`: `availableQuests(player)` + przeliczanie przy wczytaniu,
      przyjęciu, oddaniu i porzuceniu
- [ ] 19. `QuestLog.questViews()`: czytać dostępność ze snapshotu, usunąć skan
      katalogu i powielony warunek prerekwizytów
- [ ] 20. `Game.ts`: `availableQuests()` i `syncQuestMarkers()` na danych serwera
- [ ] 21. Widok oferty w dialogu: opis, cel, nagrody, `Przyjmij` / `Może później`;
      tekst `active` przy powrocie z niedokończonym zadaniem
- [ ] 22. Przyjęcie zadania przestaje zamykać okno dialogu
- [ ] 23. Porzucanie: `abandonQuest` w `GameNetwork`, handler w `QuestSystem`
      (tylko `active` / `ready_to_claim`), przycisk z potwierdzeniem w dzienniku
- [ ] 24. `notices.ts`: copy dla nowych odrzuceń, jeśli dojdą
- [ ] 25. Typecheck klient + serwer, lint
- [ ] 26. Test ścieżki: przyjmij → postęp → gotowe → oddaj → porzuć drugie zadanie
