# Plan: Class resources — rage (warrior)

Timestamp: 2026-08-12_13-41

## Cel
Zasób klasowy: warrior = **rage** (max 100). Generacja z ataków, decay poza walką (WoW). Spell `sweeping_strike` kosztuje **25** rage. Architektura pod przyszłe mana/energy.

## Zakres v1
1. Schema: `resource`, `maxResource`, `resourceKind` (`rage` | `mana` | `energy` | `none`)
2. Config: class → kind; skill → `resourceCost`
3. Serwer: gain przy auto-attack / skill hit / (opcjonalnie) otrzymanym dmg; decay po OOC; spend przed cast
4. HUD: drugi pasek (czerwony rage) tylko gdy kind ≠ none
5. Client gate + notice `not_enough_resource`

## Poza zakresem
- Mana / energy tick rates
- Talenty / bloodrage
